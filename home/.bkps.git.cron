#!/bin/bash

bkpFolders=(
  $HOME/dotfiles
  $HOME/notes
  $HOME/private
  $HOME/code/arafatm/blinkist
)


for f in ${bkpFolders[@]}; do
  if [[ -d $f ]]; then
    cd $f && echo $(pwd)
    git pull \
      && git add . \
      && git commit . -m "bkp" \
      && git push
  fi
done

